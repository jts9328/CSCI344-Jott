#this will fail phase 3 for no return
#the loop does not have to run.

Def foo[]:Integer{
    Integer x;
    x = 0;
	If[x<10]{
		::print[x];
		If[x<3]{
            Return x + 2;
        }
	}
    Else{
        Return x + 1;
    }
}

Def main[]:Void{
    Integer y;
    y = ::foo[];
}